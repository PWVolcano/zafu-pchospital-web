# 电脑医院社团官网 / 综合服务平台需求分析

> 文档类型：软件需求规格说明（SRS / PRD）  
> 文档状态：需求确认稿，可用于原型、数据库、接口与开发任务拆分  
> 适用范围：电脑医院社团官网、用户服务端、成员工作台、管理后台  
> 核心业务：清灰预约、学期内活动维修、学期外自主接单、维修备案、志愿时长管理

---

## 1. 项目概述

### 1.1 项目定位

本平台是面向在校学生和电脑医院社团成员的综合服务平台，不是传统的送修工单系统。平台以电脑清灰、散热模组清理及常见电脑问题服务为主要对象，同时承担社团展示、活动组织、维修备案、成员管理与志愿时长核算等工作。

平台按当前运行时段提供两种互斥的维修模式：

- **学期内模式**：管理员发布维修活动，用户报名活动，成员报名活动；仅报名该活动的成员可接取该活动中报名用户的维修需求。维修完成后填写学期内备案表，并据此计算志愿时长。
- **学期外模式**：普通用户直接提交报修需求，成员自主接单并根据必填联系方式私下联系用户；双方约定时间、地点后由用户自行前往，维修结束后成员填写学期外备案表，用户可自愿评价。

### 1.2 建设目标

1. 为用户提供尽可能简短、快速的电脑清灰和维修预约入口。
2. 支持学期内集中活动与学期外自主接单两套业务流程。
3. 形成真实、可追溯的维修备案记录。
4. 管理成员、活动、用户及志愿时长，并严格控制管理员和社长权限。
5. 保留官网对外展示能力，使未登录访客也能了解社团及公开活动。

### 1.3 明确不采用的旧需求

以下旧设计不再适用，开发时不得恢复：

- 不采用“接单后送修、物流式流转”的制度。
- 不设置“待接单 → 已接单 → 处理中 → 待用户确认 → 已完成”等完整维修订单生命周期。
- 不要求用户反复填写大量设备与故障信息。
- 不允许用户仅通过选择“社团成员登录”自行取得成员身份。
- 评价不对学期内活动开放。
- 学期外备案表不填写维修耗时，学期外维修不累计志愿时长。
- 社长不再是固定的永久账户角色，也不再使用单独的“转让社长”功能；社长权限由密钥和唯一在线租约决定。

---

## 2. 术语与业务对象

| 术语     | 定义                                                              |
| -------- | ----------------------------------------------------------------- |
| 访客     | 未登录网站的访问者，仅可浏览公开内容                              |
| 用户     | 已用手机号登录、但没有有效成员身份的非社团成员                    |
| 普通成员 | 已通过管理员预录入 QQ 完成成员绑定的社团成员                      |
| 管理员   | 可管理活动、用户、成员、维修和备案的成员                          |
| 社长     | 当前持有社长权限租约的管理员；全站同一时刻最多一个账号持有        |
| 维修需求 | 用户提交的简化报修/预约信息；学期内必须关联活动，学期外不关联活动 |
| 活动     | 管理员在学期内发布的集中维修服务安排                              |
| 维修备案 | 成员在实际维修结束后提交的正式维修记录                            |
| 运行模式 | 全站当前处于“学期内”或“学期外”的系统级状态                        |
| 活动时长 | 成员参加一次活动所获得的基础活动时间                              |
| 维修耗时 | 学期内单次实际维修所用时间，用于志愿时长换算                      |

---

## 3. 账户、身份与认证

### 3.1 统一账户模型

平台只建立一套用户账户，基础身份通过关联数据判断：

```text
统一 User 账户（手机号 + QQ）
├── 无有效 MemberProfile：用户
└── 有有效 MemberProfile：社团成员
    ├── MEMBER：普通成员
    └── ADMIN：管理员
        └── 持有当前 president lease：社长
```

用户后来加入社团时，应在原账户上增加成员身份，原有维修与活动相关数据必须保留，不得新建一套割裂的成员账户。

### 3.2 登录入口

登录页提供两个入口：

- 用户登录
- 社团成员登录

选择入口只决定登录后的身份校验和目标界面，不能直接赋予任何权限。

### 3.3 用户首次登录

首次以用户身份登录时填写：

- 手机号，必填；
- 短信验证码，必填；
- 本人 QQ 号，必填。

验证码校验通过且 QQ 未被其他账户占用后，创建或完成统一账户绑定。

### 3.4 成员首次登录与 QQ 匹配

管理员先在“成员管理—新增成员”中预录入成员 QQ。最简情况下只需填写 QQ，记录状态为“待绑定”。成员首次选择“社团成员登录”时填写：

- 手机号，必填；
- 短信验证码，必填；
- QQ 号，必填；
- 学号，必填；
- 班级，必填；
- 姓名，必填。

系统必须按以下顺序处理：

1. 校验手机号验证码。
2. 校验 QQ 是否存在于待绑定成员名单，或该 QQ 是否已关联当前账户的有效成员档案。
3. 若是待绑定 QQ，则将姓名、学号、班级写入成员档案并绑定当前统一账户。
4. 将待绑定状态改为已绑定，QQ 不得再绑定其他账户。
5. 若 QQ 未被管理员预录入，则拒绝获得成员身份，并提示联系管理员。

已是普通用户的账户若 QQ 被管理员加入待绑定成员名单，下次通过成员入口补齐学号、班级、姓名即可升级为成员；原账户及历史记录保持不变。

### 3.5 后续登录与登录保持

- 完成首次绑定后，用户或成员后续登录只需手机号和验证码。
- 系统应安全地保持登录状态，关闭浏览器或再次访问时无需重复登录。
- 主动退出、会话过期、账号被禁用或安全策略触发时，需要重新验证。
- 后端必须在每次受保护请求中重新校验账户状态与实际权限，不能只依赖前端保存的角色。

### 3.6 手机号与 QQ 规则

- QQ 在全站账户中唯一，一个 QQ 只能绑定一个账户。
- 用户、普通成员均不能自行修改手机号和 QQ。
- 管理员可在后台查询和修改用户、成员资料，包括手机号和 QQ；修改 QQ 前必须进行唯一性校验。
- QQ 同时承担账户联系方式与成员资格匹配凭证，任何修改都应写入操作日志。
- 社长密钥不是登录密码，不替代手机号验证码登录。

### 3.7 账号与成员状态

- 账号至少支持正常、禁用两种状态。
- 成员至少支持待绑定、有效、已撤销三种状态。
- “删除成员”在业务上应实现为“撤销成员身份”，不得物理删除其历史维修、活动、备案和志愿时长数据。
- 撤销成员身份后，该账户回到普通用户身份。

---

## 4. 角色与权限

### 4.1 权限矩阵

| 功能                     | 访客 | 用户 |    普通成员    |     管理员     |      社长      |
| ------------------------ | :--: | :--: | :------------: | :------------: | :------------: |
| 浏览公开官网内容         |  ✓   |  ✓   |       ✓        |       ✓        |       ✓        |
| 手机号验证码登录         |  —   |  ✓   |       ✓        |       ✓        |       ✓        |
| 提交学期外报修           |  —   |  ✓   |   按用户能力   |   按用户能力   |   按用户能力   |
| 报名学期内维修活动       |  —   |  ✓   | 可作为用户报名 | 可作为用户报名 | 可作为用户报名 |
| 查看本人维修记录         |  —   |  ✓   |       ✓        |       ✓        |       ✓        |
| 报名成为活动维修成员     |  —   |  —   |       ✓        |       ✓        |       ✓        |
| 学期外自主接单           |  —   |  —   |       ✓        |       ✓        |       ✓        |
| 学期内接取活动需求       |  —   |  —   | 仅已报名该活动 | 仅已报名该活动 | 仅已报名该活动 |
| 提交维修备案             |  —   |  —   |       ✓        |       ✓        |       ✓        |
| 查看本人志愿时长         |  —   |  —   |       ✓        |       ✓        |       ✓        |
| 发布和管理活动           |  —   |  —   |       —        |       ✓        |       ✓        |
| 查询、修改用户和成员信息 |  —   |  —   |       —        |       ✓        |       ✓        |
| 新增/撤销成员            |  —   |  —   |       —        |       ✓        |       ✓        |
| 管理维修需求、备案和时长 |  —   |  —   |       —        |       ✓        |       ✓        |
| 任命或取消管理员         |  —   |  —   |       —        |       —        |       ✓        |
| 切换学期内/学期外模式    |  —   |  —   |       —        |       —        |       ✓        |

说明：管理员和社长仍是统一账户，也可以在业务允许时使用普通用户/成员能力；所有敏感操作以服务端权限判断为准。

---

## 5. 全站运行模式

### 5.1 模式状态

系统全局只能处于以下一种模式：

- `IN_TERM`：学期内；
- `OUT_OF_TERM`：学期外。

只有当前社长可以切换运行模式。切换操作必须二次确认并记录操作者、切换前后状态和时间。

### 5.2 模式对功能的影响

| 能力                       | 学期内                   | 学期外                               |
| -------------------------- | ------------------------ | ------------------------------------ |
| 用户直接提交独立报修       | 关闭，改为报名已发布活动 | 开放                                 |
| 管理员发布维修活动         | 开放                     | 可管理历史活动，但不作为当前接单入口 |
| 成员自由查看并接取全站需求 | 关闭                     | 开放                                 |
| 成员报名活动               | 开放                     | 非当前流程                           |
| 活动现场接单               | 仅报名成员开放           | 关闭                                 |
| 维修评价                   | 关闭                     | 开放且选填                           |
| 维修耗时字段               | 必填                     | 不存在                               |
| 志愿时长累计               | 开放                     | 不累计                               |

### 5.3 模式切换边界

- 模式切换不得删除或改变既有数据所属模式。
- 已被接取但尚未备案的维修需求继续按创建时的模式完成，不能因全局切换而改用另一套备案表。
- 维修需求应保存创建时模式快照；学期内需求还必须保存活动关联。

---

## 6. 电脑报修与预约

### 6.1 设计原则

主要服务对象是清灰和散热模组清理，因此报修流程应尽量短。用户提交页面只强制收集完成预约所必需的信息。

### 6.2 报修字段

| 字段     | 必填 | 说明                             |
| -------- | :--: | -------------------------------- |
| 问题标题 |  是  | 简短描述需求，如“游戏本清灰”     |
| 问题类型 |  是  | 固定选项，必须包含“散热模组清理” |
| 联系方式 |  是  | 必须注明并填写微信号或 QQ 号     |
| 电脑品牌 |  否  | 可选                             |
| 电脑型号 |  否  | 可选                             |
| 操作系统 |  否  | 可选                             |
| 问题描述 |  否  | 可选                             |
| 问题图片 |  否  | 可选                             |
| 其他备注 |  否  | 可选                             |

问题类型可以保留系统、软件、网络、硬件、数据、其他等分类，但必须新增并突出“散热模组清理”。具体枚举可由后台配置，删除已被使用的类型时不得破坏历史记录。

### 6.3 联系方式校验

- 联系方式必须明确类型为“微信”或“QQ”，并填写对应号码。
- 可默认带入账户 QQ，但用户可针对本次维修填写微信或 QQ 联系方式。
- 维修成员接单后才可查看完整联系方式；未接单成员只查看完成接单判断所需的信息。

### 6.4 预约与线下服务规则

- 平台不提供上门取件、送修流转或物流能力。
- 所有维修均以预约后用户自行前往约定地点为前提。
- 学期内的时间和地点来自活动；学期外由成员接单后通过必填联系方式与用户私下商定。
- 系统只记录需求归属、接取关系和备案结果，不建立传统维修订单生命周期。

### 6.5 最小业务状态

为了防止重复接单和确保备案关联，系统可保留以下技术状态，但不得将其展示或扩展成旧式送修生命周期：

- 待接取；
- 已接取；
- 已备案；
- 已取消（仅用于异常或用户撤回）。

其中“已完成/未完成”属于备案表中的维修结果，不是物流式订单状态。

---

## 7. 学期外维修流程

### 7.1 主流程

```text
用户提交简化报修需求
        ↓
成员查看学期外待接取需求
        ↓
一名成员接单（需防止并发重复接取）
        ↓
成员根据联系方式私下联系用户
        ↓
双方约定时间和地点，用户自行前往
        ↓
实施维修
        ↓
成员提交学期外维修备案
        ↓
系统向用户显示可选评价入口
```

### 7.2 接单规则

- 学期外模式下，具有有效成员身份的成员均可接单。
- 同一维修需求同一时间只能由一名成员接取。
- 接单接口必须采用原子校验，避免两人同时成功。
- 成员接单后可查看用户提交的完整联系方式并自行联系。
- 平台不要求成员在系统中维护“处理中”等过程节点。

### 7.3 完成条件

成员提交合法的学期外备案后，该次维修视为已备案；备案结果既可为“已完成”，也可为“未完成”。两种结果均允许沉淀真实记录。

---

## 8. 学期内活动维修流程

### 8.1 活动发布

管理员可创建并发布维修活动。活动至少包含：

| 字段               | 必填 | 说明                                            |
| ------------------ | :--: | ----------------------------------------------- |
| 活动标题           |  是  | 活动名称                                        |
| 活动介绍           |  是  | 活动说明、服务范围与注意事项                    |
| 活动日期及起止时间 |  是  | 同时用于活动基础时长计算                        |
| 活动地点           |  是  | 用户自行前往的预约地点                          |
| 用户报名起止时间   |  是  | 控制用户报名窗口                                |
| 成员报名起止时间   |  是  | 控制成员报名窗口                                |
| 用户名额           |  否  | 为空表示不限制                                  |
| 成员名额           |  否  | 为空表示不限制                                  |
| 活动时长倍率       |  是  | 非负数，如 2 倍                                 |
| 维修时长倍率       |  是  | 非负数，如 3 倍                                 |
| 活动状态           |  是  | 草稿、已发布、报名中/报名结束、已结束等展示状态 |

倍率在活动发布后若需修改，必须保留历史变更并明确是否影响已结算记录；默认对已结算记录不追溯修改。

### 8.2 用户报名

- 学期内用户不提交游离于活动之外的维修需求，而是选择已发布且可报名的维修活动。
- 报名时填写与简化报修相同的必填信息：问题标题、问题类型、联系方式；其余可选。
- 报名成功后生成一条关联该活动的维修需求，`activity_id` 必填。
- 用户是否参加过活动可由后台查询，但普通用户个人中心不提供“我的活动”入口。

### 8.3 成员报名

- 有效社团成员可报名参加处于报名期的活动。
- 只有报名成功的成员，才能在该活动中查看并接取报名用户的维修需求。
- 未报名成员、普通用户不能接取活动维修需求。
- 成员报名记录应保留报名状态，供活动时长核算与后台统计使用。

### 8.4 活动接单与维修

```text
管理员发布活动
    ├── 用户报名并生成活动维修需求
    └── 成员报名活动
              ↓
活动现场：已报名成员接取该活动用户需求
              ↓
用户自行到达活动地点，成员实施维修
              ↓
成员提交学期内备案
              ↓
系统计算该成员本次活动的志愿时长
```

- 接取操作须验证活动、需求和成员三者关系。
- 一个需求只允许一名成员成功接取。
- 本期不设计多人共同维修同一需求的时长拆分；如未来需要，应另行确认规则。
- 学期内备案完成后不触发用户评价。

---

## 9. 维修备案表

### 9.1 通用规则

- 备案必须关联具体维修需求和实际接取成员。
- 成员只能为自己已接取的需求提交备案；管理员可查询和按权限纠错，但所有修改必须留痕。
- 维修照片至少上传一张，并进行文件类型、大小和安全校验。
- 维修结果使用固定单选项：**已完成**、**未完成**。
- “未完成”也允许提交，以真实记录无法处理或现场未解决的情况。

### 9.2 学期内备案表

| 字段     | 必填 | 填写方式与来源                       |
| -------- | :--: | ------------------------------------ |
| 维修人员 |  是  | 系统按当前接取成员自动导入，不可手填 |
| 用户信息 |  是  | 从活动维修需求自动导入，不可手填     |
| 维修日期 |  是  | 从活动日期自动导入，不可手填         |
| 维修耗时 |  是  | 成员填写                             |
| 维修内容 |  是  | 成员填写                             |
| 维修结果 |  是  | 单选：已完成 / 未完成                |
| 维修照片 |  是  | 至少一张                             |
| 设备品牌 |  否  | 可选                                 |
| 设备型号 |  否  | 可选                                 |
| 备注     |  否  | 可选                                 |

用户信息可展示姓名/账户标识、联系方式、需求编号和问题类型等已有数据，无需成员重复录入。

维修耗时应以整数分钟存储，以避免小数小时计算误差；界面可按“30 分钟 / 0.5 小时”等友好格式显示。

### 9.3 学期外备案表

| 字段     | 必填 | 填写方式与来源                                      |
| -------- | :--: | --------------------------------------------------- |
| 维修人员 |  是  | 当前接取成员自动导入                                |
| 用户信息 |  是  | 从维修需求自动导入                                  |
| 维修日期 |  是  | 系统按实际备案/维修日期自动生成；管理员可按权限纠错 |
| 维修内容 |  是  | 成员填写                                            |
| 维修结果 |  是  | 单选：已完成 / 未完成                               |
| 维修照片 |  是  | 至少一张                                            |
| 设备品牌 |  否  | 可选                                                |
| 设备型号 |  否  | 可选                                                |
| 维修地点 |  否  | 可选                                                |
| 备注     |  否  | 可选                                                |

**学期外备案表不包含维修耗时字段，也不产生志愿时长。**

---

## 10. 维修评价

### 10.1 开放范围

评价仅对学期外维修开放。成员提交学期外备案后，在对应用户界面弹出或显示评价入口。

### 10.2 规则

- 评价为可选，不得阻塞备案完成或用户后续使用。
- 只有该维修需求的提交用户可以评价。
- 每条学期外维修备案最多对应一条有效评价。
- 评价至少包含对维修人员的评分；可附加文字评价。
- 学期内活动维修不出现评价入口，也不写入评价数据。
- 是否允许修改或匿名展示评价属于后续产品决策，第一版可默认提交后不可修改、仅后台可见。

---

## 11. 个人中心

### 11.1 用户个人中心

普通用户个人中心应保持简洁：

- 显示登录手机号，只读；
- 显示绑定 QQ，只读；
- 查看本人提交的维修需求及备案结果；
- 对符合条件的学期外维修进行可选评价；
- 不提供资料自行修改能力；
- 不提供“我的活动”入口。

### 11.2 普通成员个人中心

除账户和维修相关记录外，成员可：

- 查看并修改昵称、班级、头像等个人资料；
- 首次成员资料默认带入登录绑定时填写的姓名和班级；
- 查看参与的活动、接取的维修及本人备案；
- 查看学期内累计志愿时长和明细；
- 查看手机号与 QQ，但不能自行修改 QQ；手机号同样不得自行修改。

姓名、学号等身份字段是否允许成员自行修改，第一版按关键身份资料处理：仅管理员修改。

### 11.3 管理员个人空间

管理员除成员个人中心能力外，还可：

- 进入管理后台；
- 在个人空间输入社长密钥以申请社长权限；
- 查看当前身份是“管理员”还是“社长”；
- 若社长权限被其他管理员取得，仍保持登录并回退为管理员。

### 11.4 社长个人空间

当前社长显示社长身份及专属入口，包括管理员任免、运行模式切换和社长相关安全设置。每次执行社长专属操作时，后端都必须校验其仍持有当前租约。

---

## 12. 管理后台

### 12.1 访问范围

管理后台仅管理员和当前社长可访问。普通成员和用户不得通过隐藏菜单、修改请求参数或直接访问接口绕过权限控制。

### 12.2 管理员功能

管理员主要职能包括：

1. **活动管理**：创建、编辑、发布、结束活动；设置报名时间、地点、人数和两类倍率；查看用户与成员报名。
2. **用户管理**：查询用户，查看和修改手机号、QQ、账号状态等资料。
3. **成员管理**：仅预录入 QQ 新增待绑定成员，查看绑定状态，修改成员资料，撤销或恢复成员身份。
4. **维修管理**：查询学期内/学期外维修需求、接取关系和异常情况。
5. **备案管理**：查询备案、在授权范围内纠错并保留修改日志。
6. **志愿时长管理**：查看活动时长、维修耗时、倍率、最终时长及计算明细；必要的人工调整必须记录原因和操作者。

管理员不能任命/取消其他管理员，不能切换系统运行模式，除非当前同时持有社长权限。

### 12.3 社长专属功能

当前社长在管理员权限基础上新增：

- 将普通成员任命为管理员；
- 将管理员取消为普通成员；
- 切换全站学期内/学期外模式；
- 管理社长相关系统配置。

不得取消当前社长自身的管理员基础身份，除非先由其他管理员取得社长权限并完成合法变更。

---

## 13. 社长密钥与唯一在线权限

### 13.1 获取社长权限

1. 管理员正常登录。
2. 在管理员个人空间选择“获取社长权限”。
3. 输入社长密钥。
4. 服务端校验密钥和管理员身份。
5. 校验成功后，将当前账号设为全站唯一的社长权限持有者。

### 13.2 唯一账号规则

- 全站同一时刻最多一个**账号**拥有社长权限。
- 同一账号可在多个已认证设备上使用社长权限，不视为冲突。
- 若不同管理员账号再次输入正确密钥，新账号立即取得社长权限，原账号立即回退为管理员。
- 原账号无需退出登录；其下一次请求或权限刷新时应收到“社长权限已由其他管理员获取，当前账号已恢复为管理员权限”的提示。

### 13.3 实现约束

- 管理员是持久基础角色，社长是临时权限租约，不应简单永久写入 `users.role = PRESIDENT`。
- 社长租约至少记录当前账号、取得时间和最近活动时间；更新必须使用事务或等效并发控制，保证全站唯一。
- 社长密钥只保存安全哈希，不得明文存储或写入普通日志。
- 任免管理员、切换运行模式等高权限接口必须实时校验当前租约。
- 登录令牌中即使包含旧的社长声明，也不得绕过实时租约校验。
- 密钥验证应具备频率限制、失败审计和防暴力尝试措施。

---

## 14. 志愿时长

### 14.1 适用范围

- 仅学期内活动中的有效成员参与记录和维修备案可累计志愿时长。
- 学期外接单和备案不累计志愿时长。
- 用户不累计志愿时长。

### 14.2 计算公式

对成员在某一活动中的最终志愿时长：

```text
V = A × Ma + Σ(Ri × Mr)
```

其中：

- `V`：最终志愿时长；
- `A`：活动基础时长；
- `Ma`：活动时长倍率；
- `Ri`：该成员在此活动中第 i 条学期内维修备案的实际维修耗时；
- `Mr`：该活动的维修时长倍率。

示例：活动基础时长 2 小时，活动倍率 2；成员一条维修记录耗时 0.5 小时，维修倍率 3：

```text
V = 2 × 2 + 0.5 × 3 = 5.5 小时
```

若成员维修多台设备，则对每条有效学期内备案的维修耗时分别求和。

### 14.3 数据与计算规则

- 活动基础时长和维修耗时在数据库中统一使用整数分钟保存，计算与展示时再换算成小时。
- 必须保存原始活动时长、每条维修耗时、活动倍率、维修倍率和计算结果，不能只保存最终总数。
- 活动倍率与维修倍率应保存到结算快照中，避免后续修改活动影响历史记录。
- 只有报名并实际参加活动的成员可获得活动基础时长；“实际参加”的确认方式由管理员在活动结束时确认。
- 维修加成仅来自该成员本人提交并有效的学期内备案。
- 备案结果为“未完成”是否计入维修耗时：第一版按实际发生的维修时间计入，但管理员可在备案审核时判定该记录无效；所有排除或调整必须留痕。
- 成员个人中心展示累计时长、按活动明细、活动部分、维修部分和最终换算结果。

---

## 15. 页面与导航清单

### 15.1 公开前台

- 首页：社团简介、核心服务、近期公开活动、报修/活动报名入口；
- 关于我们：社团介绍、服务范围、联系方式；
- 活动中心：公开活动列表与活动详情；
- 登录页：用户登录、社团成员登录两个入口。

### 15.2 用户端

- 首次登录/QQ 绑定页；
- 简化报修页（学期外）；
- 活动列表与活动报名页（学期内）；
- 维修需求详情/备案结果页；
- 可选评价弹窗或页面（仅学期外）；
- 用户个人中心。

### 15.3 成员端

- 成员首次绑定资料页；
- 学期外待接取需求列表；
- 学期内活动报名页；
- 已报名活动的现场需求列表；
- 我的接取记录；
- 学期内/学期外备案表；
- 志愿时长与明细；
- 成员个人中心。

### 15.4 管理端

- 管理概览；
- 活动管理与报名名单；
- 用户管理；
- 成员管理与待绑定 QQ；
- 维修需求管理；
- 备案管理；
- 志愿时长管理；
- 操作日志；
- 社长权限入口；
- 社长专属的管理员任免与运行模式设置。

---

## 16. 核心数据模型

以下为逻辑模型，字段名可随实际技术栈调整。

### 16.1 `users` 统一账户

- `id`
- `phone`，唯一
- `qq`，唯一、不可为空
- `status`
- `created_at`、`updated_at`

### 16.2 `member_candidates` 待绑定成员

- `id`
- `qq`，唯一
- `status`：pending / bound / revoked
- `bound_user_id`，可空
- `created_by`
- `created_at`、`bound_at`

### 16.3 `member_profiles` 成员资料

- `id`
- `user_id`，唯一
- `student_id`
- `real_name`
- `class_name`
- `nickname`，可空
- `avatar`，可空
- `member_role`：MEMBER / ADMIN
- `member_status`：active / inactive
- `created_at`、`updated_at`

### 16.4 `system_mode` 运行模式

- `mode`：IN_TERM / OUT_OF_TERM
- `changed_by`
- `changed_at`

另设模式变更历史表保存每次切换。

### 16.5 `president_lease` 社长权限租约

- `president_user_id`，全局唯一当前值
- `acquired_at`
- `last_active_at`
- `version`，用于并发控制

### 16.6 `activities` 活动

- `id`
- `title`、`description`、`location`
- `start_time`、`end_time`
- `user_signup_start`、`user_signup_end`
- `member_signup_start`、`member_signup_end`
- `user_limit`、`member_limit`，可空
- `activity_multiplier`
- `repair_multiplier`
- `status`
- `created_by`

### 16.7 `activity_member_signups` 成员活动报名

- `id`
- `activity_id`
- `member_id`
- `status`
- `attendance_confirmed`
- `signed_up_at`

同一成员对同一活动只允许一条有效报名记录。

### 16.8 `repair_requests` 维修需求

- `id`
- `user_id`
- `mode_snapshot`
- `activity_id`：学期内必填，学期外为空
- `title`
- `problem_type`
- `contact_type`：WECHAT / QQ
- `contact_value`
- `brand`、`model`、`operating_system`，可空
- `description`、`images`、`remark`，可空
- `assignment_status`：unclaimed / claimed / recorded / cancelled
- `assigned_member_id`，可空
- `created_at`、`claimed_at`

### 16.9 `repair_records` 维修备案

- `id`
- `repair_request_id`，唯一
- `member_id`
- `user_id`
- `activity_id`，学期内必填
- `mode_snapshot`
- `repair_date`
- `duration_minutes`：学期内必填，学期外必须为空
- `repair_content`
- `repair_result`：COMPLETED / NOT_COMPLETED
- `photos`
- `brand`、`model`、`location`、`remark`，按模式可空
- `created_at`、`updated_at`

### 16.10 `repair_reviews` 学期外评价

- `id`
- `repair_record_id`，唯一
- `user_id`
- `member_id`
- `rating`
- `comment`，可空
- `created_at`

数据库和服务层都必须校验关联备案为学期外。

### 16.11 `volunteer_records` 志愿时长

- `id`
- `member_id`
- `activity_id`
- `activity_minutes`
- `activity_multiplier_snapshot`
- `repair_minutes`
- `repair_multiplier_snapshot`
- `final_minutes` 或等价的高精度结果
- `status`
- `adjustment_minutes`、`adjustment_reason`，可空
- `calculated_at`

### 16.12 `audit_logs` 操作日志

- 操作者、操作类型、目标对象、变更前后摘要、时间、必要的安全上下文。
- 社长密钥、验证码、完整令牌等敏感信息不得写入日志。

---

## 17. 关键接口能力清单

接口路径由开发团队确定，第一版至少需要以下能力：

1. 发送和校验手机号验证码、刷新会话、退出登录。
2. 用户首次绑定 QQ；成员按待绑定 QQ 完成首次绑定。
3. 获取当前账号、成员资料、真实角色和当前运行模式。
4. 获取/更新允许修改的成员个人资料。
5. 创建、查看本人维修需求；按当前模式限制入口。
6. 创建活动报名维修需求；成员报名活动。
7. 成员查询可接取需求并原子接单。
8. 按模式获取正确备案表结构并提交备案。
9. 用户查询本人记录并提交学期外评价。
10. 管理员增改查用户、成员和待绑定 QQ，撤销成员身份。
11. 管理员增改查活动、报名、维修、备案和志愿记录。
12. 管理员输入社长密钥获取社长租约。
13. 社长任免管理员、切换运行模式。
14. 查询志愿时长汇总与可追溯计算明细。

所有接口都必须进行服务端鉴权、对象归属校验和模式校验。

---

## 18. 通知与提示

第一版至少提供站内即时提示或页面状态提示：

- 登录/绑定成功或失败；
- QQ 未在成员名单、QQ 已绑定其他账户；
- 报修或活动报名成功；
- 接单冲突；
- 备案提交成功；
- 学期外备案后用户出现可选评价入口；
- 社长权限获取成功或被其他管理员抢占；
- 运行模式已切换。

短信仅用于验证码；维修沟通仍通过用户填写的微信或 QQ 私下完成。其他外部通知渠道不属于当前必做范围。

---

## 19. 非功能需求

### 19.1 安全

- 验证码设置有效期、发送频率和尝试次数限制，防止滥用。
- 会话凭证使用安全 Cookie 或同等级安全方案，支持续期、吊销和退出。
- 严格采用服务端角色与对象权限校验。
- 社长密钥使用适合密码的哈希算法保存，并具备失败限流和审计。
- 上传图片限制类型、数量、大小，随机化存储名，隔离可执行内容。
- 防护 SQL 注入、XSS、CSRF、越权访问和批量枚举。
- 联系方式仅向有业务需要的接单成员和管理员展示。

### 19.2 一致性与并发

- QQ 唯一绑定、维修需求接单、活动名额、社长租约抢占必须具备数据库级约束和事务控制。
- 重复提交备案、评价或报名时应具备幂等性或明确的冲突响应。
- 模式切换和高权限操作必须保留审计记录。

### 19.3 可用性

- 支持电脑和手机端，用户报修与活动报名必须适合移动端快速操作。
- 非必填字段不得阻碍表单提交。
- 自动导入字段需清楚标识且不要求成员重复输入。
- 重要操作给出明确成功、失败和恢复提示。

### 19.4 数据保留

- 账号禁用、成员撤销和活动结束不得删除历史维修、备案、评价及志愿记录。
- 图片、联系方式等个人数据应按学校和平台隐私规则控制访问、保留和删除。

---

## 20. 核心验收标准

### 20.1 登录与成员绑定

- 新用户可用正确手机号验证码及唯一 QQ 完成首次登录。
- 已绑定账户再次登录无需重复填写 QQ、姓名、学号和班级。
- 未被管理员预录入 QQ 的用户选择成员登录时不能获得成员身份。
- 待绑定 QQ 成功绑定一次后，不能再绑定第二个账户。
- 普通用户已有账户升级为成员后，原历史记录不丢失。
- 登录状态可保持；账号禁用或退出后原会话不可继续使用。

### 20.2 学期外流程

- 学期外用户可只填写三个必填项完成报修。
- 两名成员同时接同一需求时最多一人成功。
- 成员接单后能看到联系方式并线下联系，系统不要求维护送修生命周期。
- 学期外备案不显示、也不保存维修耗时。
- 提交备案后用户可选择评分或跳过；学期外维修不增加志愿时长。

### 20.3 学期内流程

- 学期内用户通过活动报名产生关联活动的维修需求，不能创建独立需求。
- 未报名活动的成员不能接取该活动需求。
- 学期内备案自动带入维修人员、用户信息和活动日期。
- 缺少维修耗时、维修内容、维修结果或维修照片时不能提交备案。
- 学期内备案后不出现评价入口。

### 20.4 社长权限

- 只有管理员可用正确密钥获取社长权限。
- 管理员 B 获取权限后，管理员 A 的社长权限立即失效，但 A 保持管理员登录状态。
- 同一账号多设备使用不触发账号间抢占。
- 失去租约的旧令牌不能执行任免管理员或切换模式等社长操作。

### 20.5 志愿时长

- 示例数据 `2 小时 × 2 + 0.5 小时 × 3` 的结果为 `5.5 小时`。
- 多条维修备案可正确累计，原始分钟数和倍率可追溯。
- 学期外记录不进入计算。
- 修改活动倍率不应静默改变已结算历史记录。

---

## 21. 第一版范围与后续扩展

### 21.1 第一版必须完成

- 手机号验证码登录、首次 QQ 绑定和登录保持；
- 管理员预录入 QQ、成员首次绑定和统一账户升级；
- 学期内/学期外全局模式；
- 简化报修和活动报名；
- 两种模式的成员接单规则；
- 两套维修备案表；
- 仅学期外可选评价；
- 四类身份对应的个人中心与权限；
- 活动、用户、成员、维修、备案管理；
- 社长密钥、唯一社长租约、管理员任免和模式切换；
- 学期内志愿时长计算和明细展示；
- 基础官网公开页面、安全控制与操作日志。

### 21.2 非本次已确认核心范围

知识库、维修案例自动发布、招新系统、在线聊天、AI 故障诊断、积分、邮件/微信通知等可以后续单独立项，不应影响本文件所定义的核心流程，也不得在未确认时作为第一版阻塞项。

---

## 22. 待产品细化但不阻塞建模的事项

以下细节不改变核心业务，可在原型或开发评审时确定：

- 问题类型除“散热模组清理”外的最终枚举；
- 评分量表采用 5 分制或其他形式；
- 用户是否可在未接单前撤回需求，以及管理员的异常取消规则；
- 活动“实际参加”的签到/确认方式；
- 学期外维修日期自动取备案当天还是允许成员选择后由管理员审核；
- 上传照片的数量上限、单张大小和保存期限；
- 志愿时长展示的小数精度及最终导出格式。

在这些事项确认前，开发应使用可配置枚举、整数分钟和审计日志等方式保留调整空间，不得改变本文件已锁定的角色、双模式、备案、评价、QQ 匹配及志愿时长规则。

---

## 23. 最终业务总览

```text
                               当前社长
                                  │
                         切换全站运行模式
                                  │
                 ┌────────────────┴────────────────┐
                 │                                 │
              学期内                            学期外
                 │                                 │
          管理员发布活动                    用户提交简化报修
            ┌────┴────┐                           │
         用户报名   成员报名                    成员自主接单
            │         │                           │
      生成活动需求    └──────┐                    │
            └─────────────活动现场接单          私下联系预约
                          │                       │
                    用户自行到场             用户自行到约定地点
                          │                       │
                        维修                    维修
                          │                       │
                  学期内备案（含耗时）      学期外备案（无耗时）
                          │                       │
                     志愿时长                  可选评价
```

本平台后续设计与开发必须以这两条流程为主线。任何新增功能都不应重新引入送修订单生命周期，也不能绕过 QQ 成员资格匹配、活动报名限制、社长唯一租约和分模式备案规则。
